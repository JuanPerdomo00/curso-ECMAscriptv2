// Funcion Normal
function raiz(num) {
  return num ** (1 / 2);
}

// funcion flrcha con return
const raiz = (num) => {
  return num ** (1 / 2);
};

// funcion flecha sin return
// por lo general se usa cuando es un solo parametro

const raiz = (num) => num ** (1 / 2);

console.log(raiz(2));
